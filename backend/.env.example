# Obligatorio. Minimo 32 caracteres. Generar uno con:  openssl rand -base64 48
APP_JWT_SECRET=

# Origenes del frontend permitidos por CORS (coma-separados, sin barra final)
APP_CORS_ALLOWED_ORIGINS=http://localhost:5173

# Opcional: si se define, crea el usuario admin@noteapp.com en el primer arranque
APP_ADMIN_PASSWORD=
